#ifndef BASICSTAT_H
#define BASICSTAT_H

#include "graph.h"
#include "stat.h"

namespace snu {

	void basic_stat(DSGraph *graph, DSResult *result);
	void basic_stat(USGraph *graph, USResult *result);
}

#endif // BASICSTAT_H
